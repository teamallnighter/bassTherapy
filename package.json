{
  "name": "basstherapy",
  "version": "0.0.4",
  "scripts": {
    "gulp": "gulp",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run gulp watch",
    "build": "npm run gulp sass && npm run gulp scripts && ntl build",
    "deploy": "npm run gulp sass && npm run gulp scripts && ntl deploy --prod",
    "build-deploy": "npm run gulp sass && npm run gulp scripts && ntl build && ntl deploy --prod",
    "build-deploy-dev": "npm run gulp sass && npm run gulp scripts && ntl build && ntl deploy --prod && npm run gulp watch"
  },
  "devDependencies": {
    "autoprefixer": "^9.4.3",
    "browser-sync": "^2.26.3",
    "codyhouse-framework": "^2.5.4",
    "gulp": "^4.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-postcss": "^8.0.0",
    "gulp-purgecss": "^3.0.0",
    "gulp-rename": "^1.4.0",
    "gulp-sass": "^4.0.2",
    "gulp-sass-glob": "^1.1.0",
    "gulp-uglify": "^3.0.2",
    "netlify-plugin-image-optim": "^0.4.0",
    "netlify-plugin-submit-sitemap": "^0.2.3",
    "postcss-calc": "^7.0.1",
    "postcss-css-variables": "git+https://github.com/CodyHouse/postcss-css-variables"
  }
}